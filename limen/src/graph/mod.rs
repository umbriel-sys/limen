//! Optional graph helpers for P2 (typed simple chain).
//!
//! This mirrors the `limen-light` SimpleChain5 concept so a single pipeline
//! can implement P0/P1/P2 traits with minimal boilerplate.

pub mod simple_chain;
